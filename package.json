{
  "name": "tempmail-standalone",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node server.js",
    "build": "next build",
    "start": "NODE_ENV=production node server.js",
    "lint": "node --check server.js && node --check lib/*.js && node --check smtp/haraka/plugins/tempmail_inbound.js && node --check scripts/*.mjs",
    "check:env": "node scripts/check-env.mjs web",
    "check:env:smtp": "node scripts/check-env.mjs smtp",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "lucide-react": "^1.14.0",
    "next": "^16.2.6",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "sanitize-html": "^2.17.3",
    "ws": "^8.20.0"
  },
  "overrides": {
    "postcss": "^8.5.10"
  }
}
